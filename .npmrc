auto-install-peers=false
strict-peer-dependencies=false
# Vercel + pnpm monorepo: hoist deps to the project root so the
# serverless function bundler (ncc) can resolve workspace imports
# without chasing symlinks. Local dev still gets isolated installs
# from pnpm's content-addressable store.
node-linker=hoisted
